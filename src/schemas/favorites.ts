// The favorites list reuses productSummarySchema directly - a favorited row is a catalogue card, same shape the
// shop grid already renders, just filtered to what the account holder saved.
export const FAVORITES_PAGE_SIZE = 24;
